<!DOCTYPE html>
<html>
	<h1>Création d'une nouvelle experience</h1>
	
	{{ form_start(form, {'action': path('valid_experience'),'attr': {'novalidate': 'novalidate' } }) }}
	{{ form(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
</html>